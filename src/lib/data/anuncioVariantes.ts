import { AnuncioVariante } from "../types";

// Vínculos derivação → anúncio. Vazio no seed: são criados pela tela do
// anúncio ("Variações vinculadas ao anúncio") ao publicar cada derivação.

export const anuncioVariantes: AnuncioVariante[] = [];
