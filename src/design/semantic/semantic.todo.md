# Semantic — papéis não-resolvíveis (ENG-002 Etapa 2)

> GERADO. Papéis da Posição Semantic cujo conteúdo NÃO resolve a um símbolo
> declarado (alvo ausente, rampa/grupo nu, ou prosa). **Nada foi inventado.**
> Se um primitivo precisar de um destes, PARAR e pedir decisão arquitetural.

Total: 11

| Papel | Motivo |
|---|---|
| `a11y.focus.ring.color` | conteúdo Semantic em prosa: "color.border.focus" |
| `color.coach` | referência a `brand` (alvo não declarado ou rampa/grupo nu) |
| `color.origin.ai` | referência a `brand` (alvo não declarado ou rampa/grupo nu) |
| `color.surface.sunken` | referência a `base.1000` (alvo não declarado ou rampa/grupo nu) |
| `color.timeline.anchor` | referência a `info` (alvo não declarado ou rampa/grupo nu) |
| `grid.gutter` | conteúdo Semantic em prosa: "[object Object]" |
| `grid.margin` | conteúdo Semantic em prosa: "[object Object]" |
| `layout.main.max` | conteúdo Semantic em prosa: "grid.content.max" |
| `motion.duration.context-swap` | conteúdo Semantic em prosa: "motion.duration.instant" |
| `shadow.color` | conteúdo não resolvível a um símbolo pleno: "`#000000` — Valor (sem Referência)" |
| `state.disabled.icon` | referência a `color.icon.disabled` (alvo não declarado ou rampa/grupo nu) |
